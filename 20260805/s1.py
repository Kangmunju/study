### 20260804 s3.py 이어서

# --------------------------------------------------------
# 파이썬 자료구조 - 리스트 / 튜플 / 딕셔너리 / 세트
# --------------------------------------------------------

# 지금까지는 변수 하나에 값 하나만 담았음
# 이제 '여러 값을 한 변수에 담는 방법을 배움
# 반복문은 아직 배우지 않았으므로 이 챕터에서는 사용하지 않음

# 네가지를 한 눈에 보기

# 리스트    [1, 2, 3]             순서 O    수정 O    중복 O
# 튜플    (1, 2, 3)               순서 O    수정 X    중복 O
# 딕셔너리 {"a":1, "b":2} 키:값              수정 O    중복 X

# 리스트가 가장 기본이고 사용 빈도도 압도적
# 나머지 셋은 리스트와의 차이점을 위주로 이해하면 쉽다


# --------------------------------------------------------
# 1. 리스트(List) - 가장 많이 사용
# --------------------------------------------------------

# 리스트가 필요한 이유
score1 = 90  # 변수만 쓰면 사람이 늘어날 때마다 변수를 계속 생성해야 함
score2 = 85
score3 = 77
score4 = 59

scores = [90, 85, 77, 92, 80]  # 리스트로 쓰면 한 줄로 끝
print(scores)


# --------------------------------------------------------
# 리스트 만들기
# --------------------------------------------------------

# 대괄호 [] 안에 쉼표로 나열
numbers = [1, 2, 3, 4, 5]
fruits = ["사과", "바나나", "포도"]
empty = []
mixed = [1, "안녕", 3.14, True, None]  # 종류가 섞여도 가능
nested = [[1, 2], [3, 4]]  # 리스트 안에 리스트 생성 가능
print(type(nested))  # list
print(len(fruits))  # 3 -> 들어있는 요소의 개수를 셈


# --------------------------------------------------------
# 값 꺼내기 - 인덱싱
# --------------------------------------------------------

fruits = ["사과", "바나나", "포도", "딸기"]
print(fruits[0])  # 사과
print(fruits[2])  # 포도
print(fruits[-1])  # 딸기

# print(fruits[4])      # Error
print(nested[0][1])  # 2 -> 바깥 0번, 그 안의 1번

# 마지막 값은 [-1]


# --------------------------------------------------------
# 잘라내기 - 슬라이싱
# --------------------------------------------------------

numbers = [10, 20, 30, 40, 50]
print(numbers[1:3])  # [20, 30] -> 끝 번호는 포함하지 않음
print(numbers[:3])  # [10, 20, 30] -> 처음부터 끝 번호 전까지
print(numbers[2:])  # [30, 40, 50] -> 두번째부터 끝까지
print(numbers[-2:])  # [40, 50]  -> 뒤에서 2개
print(numbers[::2])  # [10, 30, 50]  -> 2칸씩 건너뛰기

# [::-1]이면 뒤집힌다
print(numbers[::-1])  # [50, 40, 30, 20, 10]
# reverse()와 달리 원본을 건드리지 않고 새 리스트를 생성
# 문자열에서도 사용 가능
# "안녕하세요"[::-1]


# --------------------------------------------------------
# 리스트는 수정이 가능 - 문자열과의 결정적인 차이
# --------------------------------------------------------

word = "PYTHON"
# word[0] = "J" 처럼 문자열은 수정이 불가

fruits = ["사과", "바나나", "포도"]
fruits[0] = "딸기"  # 리스트는 수정이 가능
print(fruits)  # ["딸기", "바나나", "포도"]


# --------------------------------------------------------
# 값 추가
# --------------------------------------------------------

fruits = ["사과", "바나나"]

fruits.append("포도")  # 맨 뒤에 하나 추가
print(fruits)  # ["사과", "바나나", "포도"]

fruits.insert(0, "딸기")  # 0번 자리에 끼워넣기
print(fruits)  # ["딸기", "사과", "바나나", "포도"]

fruits.extend(["수박", "참외"])  # 다른 리스트 풀어서 이어 붙이기
print(
    fruits
)  # ["딸기", "사과", "바나나", "포도", "수박", "참외"] -> 뒤로 추가되어 이어 붙임

# append와 extend 차이
a = [1, 2]  # 리스트를 '한 덩어리'로 넣음
a.append([3, 4])  # [1, 2 [3, 4]]
b = [1, 2]  # 안의 값을 '풀어서' 넣음
b.extend([3, 4])  # [1, 2, 3, 4]

# [0] * 5 로 초기 리스트 만들기
print([0] * 5)  # [0, 0, 0, 0, 0]
print([1, 2] + [3, 4])  # [1, 2, 3, 4]


# --------------------------------------------------------
# 값 삭제
# --------------------------------------------------------

fruits = ["사과", "바나나", "포도", "바나나"]
fruits.remove("바나나")  # 앞에 있는 값 하나만 삭제(해당하는 요소 전부 삭제하는 것 아님)
print(fruits)  # ["사과", "포도", "바나나"]

numbers = [10, 20, 30, 40]
last = numbers.pop()  # 맨 끝을 삭제하면서 그 값을 받음
print(last, numbers)  # 40 -> [10, 20, 30]

del numbers[0]  # 해당 인덱스 요소 삭제
print(numbers)  # [20, 30]

numbers.clear()  # 전체 비우기
print(numbers)  # [] -> 빈 배열 출력


# --------------------------------------------------------
# 정리
# --------------------------------------------------------

# remove(값) 삭제
# pop(위치) 위치에 있는 요소 삭제 + 삭제한 값 리턴
# del 리스트[위치]에 해당하는 요소 삭제
# clear() 전체 삭제하므로 [] 빈 배열 리턴


# --------------------------------------------------------
# 자주 쓰는 기능
# --------------------------------------------------------

scores = [90, 85, 77, 92, 68]
print(len(scores))  # 5 -> 길이(요소 개수)
print(max(scores), min(scores))  # max는 가장 큰 값, min은 가장 작은 값 리턴
print(sum(scores))  # 리스트에 있는 값 전부 합
print(sum(scores) / len(scores))  # 평균 구하기

fruits = ["사과", "바나나", "포도"]
print("사과" in fruits)  # True
print(fruits.index("사과"))  # 0
print([1, 2, 2, 3].count(2))  # 2

# in 하나로 조건문이 짧아질 수 있다!
answer = "Y"
if answer == "Y" or answer == "y" or answer == "yes":
    print("동의")

if answer in ["Y", "y", "yes"]:
    print("동의")

# index()와 remove()는 없는 값이면 Error!
# 항상 in으로 요소를 확인하는 습관이 필요
if "딸기" in fruits:
    fruits.remove("딸기")


# --------------------------------------------------------
# 정렬
# --------------------------------------------------------

numbers = [30, 10, 50, 20]

numbers.sort()  # 원본을 오름차순으로 정렬 -> 리턴값 없음
print(numbers)  # [10, 20, 30, 50]

numbers.sort(reverse=True)  # 원본을 내림차순으로 정렬
print(numbers)  # [50, 30, 20, 10]

original = [30, 10, 50, 20]
result = sorted(original)  # 새 리스트를 리턴
print(result, original)  # [10, 20, 30, 50] [30, 10, 50, 20]

# 가장 많이 하는 실수
wrong = [3, 1, 2].sort()
print(wrong)  # None

# 원본을 지키고 싶으면 sorted()
# 원본을 변경해도 상관 없다면 sort()
# 결과를 변수에 담고 싶은 경우 -> sorted()
# 정렬만 하기를 원하는 경우 -> sort()


# --------------------------------------------------------
# 문자열 리스트
# --------------------------------------------------------

fruits = "사과,바나나,포도".split(",")  # 문자열 -> 리스트
print(fruits)  # ['사과', '바나나', '포도']

print(", ".join(fruits))  # 사과, 바나나, 포도
print("-".join(fruits))  # 사과-바나나-포도

# 출력할 때 join()을 사용하면 가독성이 좋다!
print(fruits)  # ["사과", "바나나", "포도"]
print(", ".join(fruits))  # 사과, 바나나, 포도

# 단, join()은 문자열 리스트에서만 사용 가능
# 숫자인 경우에는 str()로 형 변환 필요


# --------------------------------------------------------
# 튜플(Tuple) - 바뀌지 않는 리스트
# --------------------------------------------------------

point = (10, 20)  # 소괄호 사용
color = ("빨강", "초록", "파랑")
print(type(color))  # tuple

point2 = 10, 20  # 괄호를 생략해도 튜플
print(point2)  # (10, 20)

# 주의
# 값이 하나면 쉼표를 꼭 붙어야 한다!
not_tuple = 10  # 튜플이 아닌 그냥 정수 10 -> 괄호가 계산으로 해석
yes_tuple = (10,)  # 올바른 튜플 표기

# 튜플 사용법은 리스트와 동일
color = ("빨강", "초록", "파랑")
print(color[0])  # 빨강
print(color[-1])  # 파랑


# --------------------------------------------------------
# 튜플과 리스트의 차이점
# --------------------------------------------------------

# 튜플은 수정이 불가능
# color[0] = "노랑"     -> 불가능
# color.append("노랑")      -> 불가능
# del colors[0]     -> 불가능
color = ("노랑", "보라")  # 아예 새로 대입하는 것만 가능

# 튜플을 사용하는 이유
# 실수로 바뀌면 안 되는 값을 보호하기 위해 사용!
SCREEN = (1970, 1000)
BIRTH = (2000, 5, 6)

# 튜플은 리스트보다 빠르고 가볍다!

# 튜플은 딕셔너리의 키로 사용할 수 있다!
# 리스트는 딕셔너리의 키로 사용할 수 없다!

# 이미 튜플을 이 전 수업에서 활용하였음
a, b = 1, 2
a, b = b, a  # 값 교환이 가능한 이유가 튜플 때문
print(a, b)
